const bonusPrice = 400;

let bonusTime = 30;
// let timeout = false;
document.getElementById('runBonus').addEventListener("click", bonus () {
    
    while (bonusTime > 0) {
        autoClick = autoClick * 2;
        IncreaseOnClick = IncreaseOnClick * 2;
        setInterval(BonusON()) {
            timer();
        }, 1000;
    }
    timer(){
        bonusTime--;
        return bonusTime;
    }
}
    
document.getElementById('runBonus').addEventListener("click", function () {
    setTimeout(function ()){
        bonus
    }, 360000;
}